node_modules/
backend/data/

# Environment files: never commit real secrets, keep only the example.
.env
.env.*
!.env.example

.DS_Store
*.log

# Editor swap files (can contain unsaved secrets from an open .env)
*.swp
*.swo
*.swn
